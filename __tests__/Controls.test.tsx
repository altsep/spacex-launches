import { render } from '@testing-library/react';
import { Controls } from '../src/components/controls/Controls';

describe('Controls', () => {
  it('renders', () => {
    render(<Controls />);
  });
});
